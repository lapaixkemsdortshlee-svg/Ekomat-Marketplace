-- Test de bout en bout du pipeline de déploiement automatique
-- (étape 2.6 de context/ROADMAP-AUTONOMIE.md). No-op volontaire:
-- si cette migration apparaît dans supabase_migrations.schema_migrations
-- en prod, le pipeline marche.
select 1;
